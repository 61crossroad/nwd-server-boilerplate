{
  "name": "nwd-server-boilerplate",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "local": "ts-node-dev --respawn --files src/server.ts",
    "generate": "prisma generate",
    "migrate:save": "prisma migrate dev --preview-feature",
    "migrate": "prisma migrate deploy --preview-feature"
  },
  "dependencies": {
    "@prisma/client": "^2.18.0",
    "express": "^4.17.1",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.9",
    "body-parser": "^1.19.0",
    "prisma": "^2.18.0",
    "ts-node": "^9.1.1",
    "ts-node-dev": "^1.1.1",
    "typescript": "^4.1.3"
  }
}
